<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uplabdhisingh.t95">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EnterRecordsActivity" />
        <activity android:name=".ViewRecordsActivity" />
        <activity android:name=".EmployeeWorklogActivity" />
        <activity android:name=".LodgeComplaintActivity" />
        <activity android:name=".EnterRecordDetails" />
        <activity android:name=".RecordDetailsActivity" />
        <activity android:name=".EmployeeWorklogDetails" />
        <activity android:name=".LodgeComplaintDetails"></activity>
    </application>

</manifest>